listen_addr: "127.0.0.1:7878"    # pip points here (localhost only)
peer_addr:   "0.0.0.0:7879"      # LAN peers connect here (all interfaces)
cache_dir:   "~/.p2p-cache"
max_cache_size_gb: 20
upstream_registry: "https://pypi.org"
audit_log: ""
org_secret: ""